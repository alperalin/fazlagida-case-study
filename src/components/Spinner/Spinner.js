// Styles
import './Spinner.scss';

function Spinner() {
  return (
    <span className="spinner" data-testid="spinner" title="Loading animation">
      Spinner
    </span>
  );
}

export default Spinner;
